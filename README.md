# retail-app
A lightweight Micronaut-based retail application that manages two core entities: Items and Suppliers. Built with Java and backed by PostgreSQL, the project provides clean, efficient CRUD APIs. The entire system is containerized using Docker for easy setup, deployment, and scalability.
